apiVersion: apps/v1
kind: Deployment
metadata:
  name: apache2-deployment
  labels:
    app: apache2
spec:
  replicas: 3
  selector:
    matchLabels:
      app: apache2
  template:
    metadata:
      labels:
        app: apache
    spec:
      containers:
      - name: apache2-pod
        image: dockerkumargaurav/prectice-ubuntu
        ports:
        - containerPort: 80
